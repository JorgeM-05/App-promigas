package com.promigasapp.apppromigas.repository;

import com.promigasapp.apppromigas.entity.CountryEntity;
//import com.promigasapp.apppromigas.entity.OpportunitiesEntity;
//import org.springframework.data.jpa.repository.JpaRepository;
//import org.springframework.data.jpa.repository.Query;
//import org.springframework.data.jpa.repository.config.EnableJpaRepositories;
//import org.springframework.data.repository.query.Param;
//import org.springframework.stereotype.Repository;
//
//import java.util.List;
//
//@Repository
////@EnableJpaRepositories
//public interface ExampleQuery extends JpaRepository<OpportunitiesEntity, Long> {
//
////    public CountryEntity findBypais(String country);
//
////    @Query(value = "SELECT op FROM oportunidades op WHERE op.id_pais LIKE %:filtro% ")
////    List<OpportunitiesEntity> search(@Param("filtro") int countries);
//
////    @Query(value = "SELECT o FROM CountryEntity c, OpportunitiesEntity o WHERE c.pais = ?1 AND c.tipoProyecto = ?2 AND o.idpais= ")
////    public OpportunitiesEntity search(String pais,String tipo);
//}
